// todo error handler in plugin::recover
#[derive(Debug, Clone)]
pub struct Config {
}

impl Default for Config {
    fn default() -> Self {
        Self {}
    }
}

#[derive(Debug, Clone)]
pub struct Static {

}

impl Default for Static {
    fn default() -> Self {
        todo!()
    }
}